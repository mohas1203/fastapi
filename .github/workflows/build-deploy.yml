name: Build and Deploy API

on: [push, pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: hello shaheed
        run: echo "hello shaheed"
